<?php

// all these stuff will now be handled by the_checker.php so its not necessiary anymore , but i didnt wanted to delete it so jsut commenting it






// require "db.php";
// $playerId = $_POST["player_id"];
// $difficulty = $_POST["difficulty"];
// $score = $_POST["score"];
// $attempts = $_POST["attempts"];
// $won = $_POST["won"];

// if($playerId == "" || $difficulty == ""){
//     echo json_encode(["error" => "oh no , missing data"]);
//     exit;
// }
// $stmt = $conn->prepare("INSERT INTO guess_the_number_scores (player_id, difficulty, score, attempts, won) VALUES (?, ?, ?, ?, ?)");
// $stmt->bind_param("isiii", $playerId, $difficulty, $score, $attempts, $won);
// $stmt->execute();

// echo json_encode(["success" => true]);
?>